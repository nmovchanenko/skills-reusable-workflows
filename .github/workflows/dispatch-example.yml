name: Example repo dispatch

on:
  repository_dispatch:
    types: [promoteToStaging]

jobs:
  example-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Event Information
        run: |
          echo "Event '${{ github.event.action }}' received from '${{ github.event.client_payload }}'"